import React from 'react';

const Post = () => {
    return (
        <div className={'card'}>
            <div className={'card-body'}>
                <p className={'card-title'}>пост</p>
            </div>
        </div>
    );
};

export default Post;
